# Strict on purpose: in protocol code an ignored error is a silent failure.
version: "2"
linters:
  enable:
    - misspell
  exclusions:
    rules:
      - linters:
          - errcheck
        path: _test\.go
run:
  timeout: 3m
